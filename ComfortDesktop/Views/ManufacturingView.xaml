<UserControl x:Class="ComfortDesktop.views.ManufacturingView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ComfortDesktop.views"
        mc:Ignorable="d"
        d:DesignHeight="600" d:DesignWidth="1200" Background="white">


        <UserControl.Resources>
            <!-- Modern Card Style -->
            <Style x:Key="ModernCardStyle" TargetType="Border">
                <Setter Property="Background" Value="White"/>
                <Setter Property="BorderBrush" Value="#E1E8FF"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="12"/>
                <Setter Property="Padding" Value="20"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect ShadowDepth="3" Opacity="0.08" BlurRadius="10" Direction="270"/>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Header Text Style -->
            <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="18"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="#355CBD"/>
                <Setter Property="Margin" Value="0,0,0,15"/>
            </Style>

            <!-- Modern DataGrid Style -->
            <Style x:Key="ModernDataGridStyle" TargetType="DataGrid">
                <Setter Property="Background" Value="White"/>
                <Setter Property="BorderBrush" Value="#E1E8FF"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="RowBackground" Value="White"/>
                <Setter Property="AlternatingRowBackground" Value="#F8FAFF"/>
                <Setter Property="HorizontalGridLinesBrush" Value="#F0F4FF"/>
                <Setter Property="VerticalGridLinesBrush" Value="#F0F4FF"/>
                <Setter Property="HeadersVisibility" Value="Column"/>
                <Setter Property="AutoGenerateColumns" Value="False"/>
                <Setter Property="CanUserAddRows" Value="False"/>
                <Setter Property="CanUserDeleteRows" Value="False"/>
                <Setter Property="CanUserReorderColumns" Value="False"/>
                <Setter Property="CanUserResizeColumns" Value="True"/>
                <Setter Property="CanUserSortColumns" Value="True"/>
                <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
                <Setter Property="ScrollViewer.PanningMode" Value="Both"/>
               
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type DataGrid}">
                            <Border BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}" 
                                SnapsToDevicePixels="True"
                                CornerRadius="8">
                                <ScrollViewer x:Name="DG_ScrollViewer" Focusable="false">
                                    <ScrollViewer.Template>
                                        <ControlTemplate TargetType="{x:Type ScrollViewer}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="*"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>

                                                <Button Command="{x:Static DataGrid.SelectAllCommand}" 
                                                    Width="{Binding CellsPanelHorizontalOffset, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"
                                                    Focusable="false"
                                                    Visibility="{Binding HeadersVisibility, ConverterParameter={x:Static DataGridHeadersVisibility.All}, Converter={x:Static DataGrid.HeadersVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" 
                                                    Background="Transparent"
                                                    BorderBrush="Transparent"
                                                    VerticalAlignment="Top"/>

                                                <DataGridColumnHeadersPresenter x:Name="PART_ColumnHeadersPresenter"
                                                                            Grid.Column="1"
                                                                            Visibility="{Binding HeadersVisibility, ConverterParameter={x:Static DataGridHeadersVisibility.Column}, Converter={x:Static DataGrid.HeadersVisibilityConverter}, RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}"/>

                                                <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" 
                                                                    Grid.Row="1" 
                                                                    Grid.ColumnSpan="2" 
                                                                    CanContentScroll="{TemplateBinding CanContentScroll}" />

                                                <ScrollBar x:Name="PART_VerticalScrollBar" 
                                                       Grid.Row="1" 
                                                       Grid.Column="2" 
                                                       Orientation="Vertical"
                                                       Maximum="{TemplateBinding ScrollableHeight}"
                                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                                       Value="{Binding VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                                            </Grid>
                                        </ControlTemplate>
                                    </ScrollViewer.Template>
                                    <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                </ScrollViewer>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- DataGrid Column Header Style -->
            <Style x:Key="DataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="#355CBD"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="Padding" Value="10,8"/>
                <Setter Property="BorderThickness" Value="0"/>
            </Style>

            <!-- DataGrid Cell Style -->
            <Style x:Key="DataGridCellStyle" TargetType="DataGridCell">
                <Setter Property="Padding" Value="10,8"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Style.Triggers>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="#E8EEFF"/>
                        <Setter Property="Foreground" Value="#355CBD"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

        </UserControl.Resources>

        <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
            <StackPanel>
                <!-- Header Section -->
                <Border Style="{StaticResource ModernCardStyle}" Margin="0,0,0,25">
                    <StackPanel>
                        <TextBlock Text="🏭 Производственные данные" 
                               FontSize="24" 
                               FontWeight="Bold" 
                               Foreground="#355CBD"
                               Margin="0,0,0,10"/>
                        <TextBlock Text="Управление и мониторинг производственных процессов" 
                               FontSize="14" 
                               Foreground="#6B8CE8"
                               FontWeight="Medium"/>
                    </StackPanel>
                </Border>

                <!-- KPI Cards -->
                <WrapPanel Margin="0,0,0,25" HorizontalAlignment="Center">
                    <Border Style="{StaticResource ModernCardStyle}" Margin="5" Width="180" Height="100">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="📦" FontSize="16" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                            <TextBlock Text="Всего продуктов" FontSize="11" Foreground="#6B8CE8" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding TotalProducts}" FontSize="16" FontWeight="Bold" Foreground="#355CBD" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                <Border Style="{StaticResource ModernCardStyle}" Margin="5" Width="180" Height="100">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="🏗️" FontSize="16" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                            <TextBlock Text="Активных цехов" FontSize="11" Foreground="#6B8CE8" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding ActiveWorkshops}" FontSize="16" FontWeight="Bold" Foreground="#355CBD" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                <Border Style="{StaticResource ModernCardStyle}" Margin="5" Width="180" Height="100">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="⏱️" FontSize="16" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                            <TextBlock Text="Ср. время (ч)" FontSize="11" Foreground="#6B8CE8" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding AverageTime}" FontSize="16" FontWeight="Bold" Foreground="#355CBD" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                <Border Style="{StaticResource ModernCardStyle}" Margin="5" Width="180" Height="100">
                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                            <TextBlock Text="👥" FontSize="16" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                            <TextBlock Text="Всего рабочих" FontSize="11" Foreground="#6B8CE8" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding TotalWorkers}" FontSize="16" FontWeight="Bold" Foreground="#355CBD" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </WrapPanel>

                <!-- Manufacturing Data Table -->
                <Border Style="{StaticResource ModernCardStyle}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <TextBlock Text="📋" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Производственные данные" Style="{StaticResource SectionHeaderStyle}"/>
                        </StackPanel>

                        <DataGrid ItemsSource="{Binding ManufacturingData}"
                              Style="{StaticResource ModernDataGridStyle}"
                              ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                              CellStyle="{StaticResource DataGridCellStyle}"
                              Height="400">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Продукт" 
                                                Binding="{Binding ProductName}" 
                                                Width="140"/>
                                <DataGridTextColumn Header="Цех" 
                                                Binding="{Binding WorkshopName}" 
                                                Width="120"/>
                                <DataGridTextColumn Header="Тип цеха" 
                                                Binding="{Binding WorkshopType}" 
                                                Width="110"/>
                                <DataGridTextColumn Header="Время (ч)" 
                                                Binding="{Binding ManufacturingTimeHours}" 
                                                Width="90"/>
                                <DataGridTextColumn Header="Рабочих" 
                                                Binding="{Binding WorkersCount}" 
                                                Width="80"/>
                                <DataGridTextColumn Header="Тип продукции" 
                                                Binding="{Binding ProductType}" 
                                                Width="130"/>
                                <DataGridTextColumn Header="Материал" 
                                                Binding="{Binding MaterialType}" 
                                                Width="120"/>
                                <DataGridTextColumn Header="Цена" 
                                                Binding="{Binding MinPartnerPrice, StringFormat=C2}" 
                                                Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </UserControl>

