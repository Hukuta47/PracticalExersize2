<UserControl x:Class="ComfortDesktop.views.CalculationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ComfortDesktop.views"
        mc:Ignorable="d"
         d:DesignHeight="600" d:DesignWidth="1200" Background="white">

    <UserControl.Resources>
        <!-- Modern Card Style -->
        <Style x:Key="ModernCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E1E8FF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="25"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="3" Opacity="0.08" BlurRadius="10" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Header Text Style -->
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#355CBD"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>

        <!-- Input Label Style -->
        <Style x:Key="InputLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#355CBD"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <!-- Modern ComboBox Style -->
        <Style x:Key="ModernComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="42"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#E1E8FF"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#355CBD"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#355CBD"/>
                </Trigger>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#355CBD"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="42"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#E1E8FF"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#355CBD"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#355CBD"/>
                </Trigger>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#355CBD"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#355CBD"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Border.CornerRadius" Value="8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2A4A9C"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#1A3A8F"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
        <StackPanel>
            <!-- Header Section -->
            <Border Style="{StaticResource ModernCardStyle}" Margin="0,0,0,25">
                <StackPanel>
                    <TextBlock Text="🧮 Расчет сырья" 
                               FontSize="24" 
                               FontWeight="Bold" 
                               Foreground="#355CBD"
                               Margin="0,0,0,10"/>
                    <TextBlock Text="Расчет необходимого количества сырья для производства" 
                               FontSize="14" 
                               Foreground="#355CBD"
                               FontWeight="Medium"/>
                </StackPanel>
            </Border>

            <!-- Main Content Grid -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Panel - Input Parameters -->
                <Border Grid.Column="0" Style="{StaticResource ModernCardStyle}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                            <TextBlock Text="📝" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Параметры расчета" Style="{StaticResource SectionHeaderStyle}"/>
                        </StackPanel>

                        <!-- Product Type -->
                        <StackPanel Margin="0,0,0,20">
                            <TextBlock Text="Тип продукции *" Style="{StaticResource InputLabelStyle}"/>
                            <ComboBox ItemsSource="{Binding ProductTypes}"
                                      DisplayMemberPath="ProductTypeName"
                                      SelectedValuePath="ProductTypeID"
                                      SelectedValue="{Binding SelectedProductTypeId}"
                                      Style="{StaticResource ModernComboBoxStyle}"/>
                        </StackPanel>

                        <!-- Material Type -->
                        <StackPanel Margin="0,0,0,20">
                            <TextBlock Text="Тип материала *" Style="{StaticResource InputLabelStyle}"/>
                            <ComboBox ItemsSource="{Binding MaterialTypes}"
                                      DisplayMemberPath="MaterialTypeName"
                                      SelectedValuePath="MaterialTypeID"
                                      SelectedValue="{Binding SelectedMaterialTypeId}"
                                      Style="{StaticResource ModernComboBoxStyle}"/>
                        </StackPanel>

                        <!-- Quantity -->
                        <StackPanel Margin="0,0,0,20">
                            <TextBlock Text="Количество продукции *" Style="{StaticResource InputLabelStyle}"/>
                            <TextBox Text="{Binding Quantity}" 
                                     Style="{StaticResource ModernTextBoxStyle}"/>
                        </StackPanel>

                        <!-- Parameters Grid -->
                        <Grid Margin="0,0,0,25">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Длина/Размер 1 (м) *" Style="{StaticResource InputLabelStyle}"/>
                                <TextBox Text="{Binding Param1}" 
                                         Style="{StaticResource ModernTextBoxStyle}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Ширина/Размер 2 (м) *" Style="{StaticResource InputLabelStyle}"/>
                                <TextBox Text="{Binding Param2}" 
                                         Style="{StaticResource ModernTextBoxStyle}"/>
                            </StackPanel>
                        </Grid>

                        <!-- Calculate Button -->
                        <Button Content="Рассчитать" 
                                Command="{Binding CalculateCommand}"
                                Style="{StaticResource ModernButtonStyle}"/>
                    </StackPanel>
                </Border>

                <!-- Right Panel - Results -->
                <Border Grid.Column="2" Style="{StaticResource ModernCardStyle}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                            <TextBlock Text="📊" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Результат расчета" Style="{StaticResource SectionHeaderStyle}"/>
                        </StackPanel>

                        <!-- Result Display -->
                        <Border Background="#F0F5FF" 
                                BorderBrush="#E1E8FF" 
                                BorderThickness="2"
                                CornerRadius="12"
                                Padding="30"
                                Margin="0,0,0,25">
                            <StackPanel HorizontalAlignment="Center" 
                                        VerticalAlignment="Center">
                                <TextBlock Text="{Binding Result}" 
                                           FontSize="42" 
                                           FontWeight="Bold" 
                                           Foreground="#355CBD"
                                           HorizontalAlignment="Center"/>
                                <TextBlock Text="единиц сырья" 
                                           FontSize="16" 
                                           Foreground="#6B8CE8"
                                           FontWeight="Medium"
                                           HorizontalAlignment="Center"
                                           Margin="0,8,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Additional Info -->
                        <StackPanel Margin="0,25,0,0">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="💡" FontSize="12" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Примечания:" 
                                           FontSize="12" 
                                           FontWeight="SemiBold"
                                           Foreground="#355CBD"/>
                            </StackPanel>
                            <TextBlock Text="• Все параметры обязательны для заполнения&#x0a;• Результат отображается в выбранных единицах измерения&#x0a;• Учитывается стандартный процент потерь 5%"
                                       FontSize="11" 
                                       Foreground="#355CBD"
                                       LineHeight="16"
                                       TextWrapping="Wrap"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
